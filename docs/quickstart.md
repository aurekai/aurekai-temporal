# Quickstart — aurekai-temporal

Aurekai pipeline as a Temporal workflow.

## Requirements

- Python 3.10+
- `pip install -r requirements.txt`
- Temporal server running locally (`temporal server start-dev`)

## Validate

```bash
bash tests/validate-schemas.sh
bash tests/validate-scripts.sh
```
